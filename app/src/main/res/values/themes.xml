<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MuscleApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MuscleApp" parent="Base.Theme.MuscleApp" />

    <style name="AlertDialogTheme" parent="Theme.Material3.Dark.Dialog.Alert">
        <item name="android:windowBackground">#1A1A1A</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorAccent">@color/red</item>
        <item name="android:colorAccent">@color/red</item>
    </style>
</resources>
